import { IAfiliado } from "./IAfiliado"
import { IServicio } from "./IServicio";

export class IAfiliadoServicio {
    afiliadoDTO: IAfiliado = new IAfiliado();
    servicioDTO: IServicio = new IServicio();
}